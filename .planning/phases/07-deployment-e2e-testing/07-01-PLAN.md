---
phase: 07-deployment-e2e-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.json
  - eas.json
  - src/hooks/use-update-check.ts
  - src/hooks/use-update-check.test.ts
  - app/_layout.tsx
  - jest.setup.js
autonomous: true
requirements: [TOOL-03]

must_haves:
  truths:
    - "app.json has expo-updates URL, fingerprint runtimeVersion, and checkAutomatically ON_ERROR_RECOVERY"
    - "eas.json has channel property on all three build profiles (development, preview, production)"
    - "useUpdateCheck hook checks for updates on app foreground, shows sonner toast when available"
    - "useUpdateCheck skips update check in __DEV__ mode"
    - "Root layout calls useUpdateCheck()"
  artifacts:
    - path: "src/hooks/use-update-check.ts"
      provides: "OTA update foreground check hook"
      exports: ["useUpdateCheck"]
    - path: "src/hooks/use-update-check.test.ts"
      provides: "Unit test for update check hook"
      min_lines: 30
    - path: "app.json"
      provides: "expo-updates configuration"
      contains: "fingerprint"
    - path: "eas.json"
      provides: "Channel configuration for 3 profiles"
      contains: "channel"
  key_links:
    - from: "app/_layout.tsx"
      to: "src/hooks/use-update-check.ts"
      via: "useUpdateCheck() hook call"
      pattern: "useUpdateCheck"
    - from: "src/hooks/use-update-check.ts"
      to: "expo-updates"
      via: "Updates.checkForUpdateAsync"
      pattern: "checkForUpdateAsync"
    - from: "src/hooks/use-update-check.ts"
      to: "sonner-native"
      via: "toast.success notification"
      pattern: "toast\\.success"
---

<objective>
Configure expo-updates for OTA JavaScript updates with fingerprint-based runtime versioning, three update channels (production/preview/development), and a foreground update check hook that shows a subtle toast when an update is available.

Purpose: Enable shipping JS-only changes instantly via EAS Update without app store review, with built-in safety from fingerprint-based native code change detection and automatic rollback on crash.
Output: Configured app.json/eas.json, working useUpdateCheck hook with unit test, root layout integration.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deployment-e2e-testing/07-CONTEXT.md
@.planning/phases/07-deployment-e2e-testing/07-RESEARCH.md

@app.json
@eas.json
@app/_layout.tsx
@jest.setup.js
@src/hooks/use-update-check.ts (new file)

<interfaces>
<!-- Existing interfaces the executor needs -->

From app/_layout.tsx:
```typescript
// Root layout with ConvexProvider, GestureHandlerRootView, WebContainer
// Hook call goes after existing useEffect hooks, before the if (!loaded) return
export default function RootLayout() {
  // ... existing hooks (useFonts, useRouter, useSegments, isOnboardingComplete)
  // ADD: useUpdateCheck() here
}
```

From sonner-native (already installed):
```typescript
import { toast } from "sonner-native";
toast.success(message: string, options?: { duration?: number }): void;
```

From expo-updates (to be installed):
```typescript
import * as Updates from "expo-updates";
Updates.checkForUpdateAsync(): Promise<{ isAvailable: boolean }>;
Updates.fetchUpdateAsync(): Promise<UpdateFetchResult>;
// Update auto-applies on next app launch -- no need to call reloadAsync()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install expo-updates and configure app.json + eas.json</name>
  <files>app.json, eas.json</files>
  <read_first>
    - app.json (current Expo config)
    - eas.json (current EAS build profiles)
    - .planning/phases/07-deployment-e2e-testing/07-RESEARCH.md (Pattern 2 and Pattern 3 for exact config)
  </read_first>
  <behavior>
    - app.json must contain runtimeVersion.policy = "fingerprint" (per D-03)
    - app.json must contain updates.url with EAS Update URL pattern (per D-02)
    - app.json must contain updates.enabled = true
    - app.json must contain updates.checkAutomatically = "ON_ERROR_RECOVERY" (custom hook handles foreground checks)
    - app.json must contain extra.eas.projectId placeholder (user fills in actual ID)
    - eas.json development profile must have channel = "development" (per D-01)
    - eas.json preview profile must have channel = "preview" (per D-01)
    - eas.json production profile must have channel = "production" (per D-01)
  </behavior>
  <action>
    1. Install expo-updates: `bun add expo-updates@~55.0.15`
    2. Update app.json to add per D-03 fingerprint runtime versioning:
       - Add `"runtimeVersion": { "policy": "fingerprint" }` at expo root level
       - Add `"updates"` block with `"url": "https://u.expo.dev/YOUR_PROJECT_ID"`, `"enabled": true`, `"checkAutomatically": "ON_ERROR_RECOVERY"`
       - Add `"extra": { "eas": { "projectId": "YOUR_PROJECT_ID" } }` -- use literal `YOUR_PROJECT_ID` as a placeholder that the user replaces after running `eas update:configure` or copying from expo.dev dashboard
       - Preserve ALL existing app.json config (icon, splash, plugins, etc.)
    3. Update eas.json per D-01 to add channel to each build profile:
       - development: add `"channel": "development"`
       - preview: add `"channel": "preview"`
       - production: add `"channel": "production"`
       - Preserve ALL existing eas.json config (distribution, buildType, etc.)
  </action>
  <verify>
    <automated>node -e "const a=require('./app.json'); const e=require('./eas.json'); console.assert(a.expo.runtimeVersion.policy==='fingerprint','missing fingerprint'); console.assert(a.expo.updates.enabled===true,'updates not enabled'); console.assert(a.expo.updates.checkAutomatically==='ON_ERROR_RECOVERY','wrong checkAutomatically'); console.assert(e.build.development.channel==='development','missing dev channel'); console.assert(e.build.preview.channel==='preview','missing preview channel'); console.assert(e.build.production.channel==='production','missing prod channel'); console.log('All config checks passed')"</automated>
  </verify>
  <acceptance_criteria>
    - expo-updates@~55.0.15 appears in package.json dependencies
    - app.json has runtimeVersion.policy = "fingerprint", updates block with url/enabled/checkAutomatically, extra.eas.projectId placeholder
    - eas.json has channel property on all three build profiles
    - No existing config is lost or overwritten
  </acceptance_criteria>
  <done>app.json and eas.json fully configured for OTA updates with fingerprint safety and 3 channels; expo-updates package installed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create useUpdateCheck hook, wire into root layout, add unit test</name>
  <files>src/hooks/use-update-check.ts, src/hooks/use-update-check.test.ts, app/_layout.tsx, jest.setup.js</files>
  <read_first>
    - app/_layout.tsx (root layout to add hook call)
    - jest.setup.js (need to add expo-updates mock)
    - .planning/phases/07-deployment-e2e-testing/07-RESEARCH.md (Pattern 1 for hook implementation)
  </read_first>
  <behavior>
    - Test 1: In __DEV__ mode, no AppState listener is registered and no update check happens
    - Test 2: When AppState changes to "active" and update is available, fetchUpdateAsync is called and toast.success shows
    - Test 3: When AppState changes to "active" and no update available, fetchUpdateAsync is NOT called
    - Test 4: When checkForUpdateAsync throws, error is caught silently (no crash)
    - Test 5: Cleanup removes AppState listener on unmount
  </behavior>
  <action>
    1. Add expo-updates mock to jest.setup.js:
       ```javascript
       jest.mock("expo-updates", () => ({
         checkForUpdateAsync: jest.fn(),
         fetchUpdateAsync: jest.fn(),
         reloadAsync: jest.fn(),
       }));
       ```
    2. Create src/hooks/use-update-check.ts per D-02:
       - Import `* as Updates from "expo-updates"`, `{ useEffect } from "react"`, `{ AppState } from "react-native"`, `{ toast } from "sonner-native"`
       - Export function `useUpdateCheck()`:
         - Early return if `__DEV__` is true (per Pitfall 5 from research)
         - Register AppState "change" listener in useEffect
         - On "active" state: try `checkForUpdateAsync()`, if `isAvailable` then `fetchUpdateAsync()` and show `toast.success("Update downloaded. Restart to apply.", { duration: 4000 })`
         - Catch block: `console.error("Update check failed:", error)` -- silent failure, updates are non-critical
         - Do NOT call `Updates.reloadAsync()` -- update applies on next natural launch (anti-pattern from research)
         - Return cleanup function that removes the AppState subscription
         - Per D-04: automatic rollback is built into expo-updates, no custom logic needed
    3. Create src/hooks/use-update-check.test.ts:
       - Mock `expo-updates` at test level (checkForUpdateAsync, fetchUpdateAsync)
       - Mock `sonner-native` toast
       - Mock `AppState.addEventListener` to capture the callback
       - Test that in dev mode (__DEV__ = true), no listener is added
       - Test that on foreground with available update, fetch + toast are called
       - Test that on foreground with no update, fetch is NOT called
       - Test that errors are caught silently
       - Test cleanup removes listener
    4. Update app/_layout.tsx:
       - Add import: `import { useUpdateCheck } from "../src/hooks/use-update-check";`
       - Add `useUpdateCheck();` call inside RootLayout function body, after the existing `const onboardingDone = isOnboardingComplete();` line and before the useEffect blocks
    5. Update jest.setup.js:
       - Add the expo-updates mock block (before the existing mocks or after the last mock)
  </action>
  <verify>
    <automated>bun run test -- --testPathPattern use-update-check --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - useUpdateCheck hook exists and exports correctly
    - All 5 test behaviors pass
    - app/_layout.tsx imports and calls useUpdateCheck()
    - jest.setup.js has expo-updates mock
    - Hook guards against __DEV__ mode
    - Hook does NOT force reload (no reloadAsync call)
  </acceptance_criteria>
  <done>useUpdateCheck hook checks for OTA updates on app foreground, shows sonner toast, is wired into root layout, and has passing unit tests</done>
</task>

</tasks>

<verification>
1. `node -e "const a=require('./app.json'); console.log('runtimeVersion:', a.expo.runtimeVersion); console.log('updates:', a.expo.updates)"` shows fingerprint policy and update config
2. `node -e "const e=require('./eas.json'); Object.entries(e.build).forEach(([k,v]) => console.log(k, 'channel:', v.channel))"` shows 3 channels
3. `bun run test -- --testPathPattern use-update-check --no-coverage` passes all tests
4. `grep -n "useUpdateCheck" app/_layout.tsx` confirms hook is wired in
5. `grep -n "expo-updates" jest.setup.js` confirms mock exists
</verification>

<success_criteria>
- expo-updates@~55.0.15 installed
- app.json has fingerprint runtimeVersion, updates URL, checkAutomatically ON_ERROR_RECOVERY
- eas.json has channel on all 3 build profiles (development, preview, production)
- useUpdateCheck hook handles foreground check with toast notification
- Root layout calls useUpdateCheck()
- Unit tests pass for the hook
- No forced reload -- update applies on next launch
</success_criteria>

<output>
After completion, create `.planning/phases/07-deployment-e2e-testing/07-01-SUMMARY.md`
</output>
