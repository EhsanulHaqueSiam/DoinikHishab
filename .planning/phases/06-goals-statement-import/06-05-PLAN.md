---
phase: 06-goals-statement-import
plan: 05
type: execute
wave: 3
depends_on: ["06-03", "06-04"]
files_modified:
  - src/components/goals/GoalDashboardCard.tsx
  - app/(tabs)/index.tsx
  - app/(tabs)/settings.tsx
  - app/(tabs)/transactions.tsx
  - app/(tabs)/budget.tsx
autonomous: false
requirements: [GOAL-02, GOAL-05, IMPT-01]

must_haves:
  truths:
    - "Dashboard shows Goals card with top 2-3 goals and mini progress bars"
    - "Tapping Goals dashboard card navigates to goals screen"
    - "Settings screen has 'Import Statement' link that navigates to import screen"
    - "Transactions screen has 'Import' button that navigates to import screen"
    - "Budget view shows goal contribution categories from useGoals hook"
  artifacts:
    - path: "src/components/goals/GoalDashboardCard.tsx"
      provides: "Dashboard widget showing top goals with mini progress bars"
      min_lines: 40
    - path: "app/(tabs)/index.tsx"
      provides: "Dashboard screen with GoalDashboardCard integrated"
    - path: "app/(tabs)/settings.tsx"
      provides: "Settings screen with Import Statement row"
    - path: "app/(tabs)/transactions.tsx"
      provides: "Transactions screen with Import button"
  key_links:
    - from: "app/(tabs)/index.tsx"
      to: "src/components/goals/GoalDashboardCard.tsx"
      via: "GoalDashboardCard component"
      pattern: "import.*GoalDashboardCard"
    - from: "src/components/goals/GoalDashboardCard.tsx"
      to: "src/hooks/use-goals.ts"
      via: "useGoals hook for goal data"
      pattern: "import.*useGoals"
    - from: "app/(tabs)/budget.tsx"
      to: "src/hooks/use-goals.ts"
      via: "useGoals hook for goalBudgetCategories"
      pattern: "import.*useGoals"
---

<objective>
Wire goals and import features into the existing app navigation. Add GoalDashboardCard to the dashboard, add "Import Statement" to settings, add "Import" button to transactions screen, and integrate goal budget categories into the budget view.

Purpose: Connects the independently-built goals and import features to the main app shell, making them discoverable and accessible to users.
Output: Dashboard card, navigation links, budget integration, and human verification of the complete phase.
</objective>

<execution_context>
@/home/siam/.claude/get-shit-done/workflows/execute-plan.md
@/home/siam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-goals-statement-import/06-CONTEXT.md
@.planning/phases/06-goals-statement-import/06-UI-SPEC.md
@.planning/phases/06-goals-statement-import/06-03-SUMMARY.md
@.planning/phases/06-goals-statement-import/06-04-SUMMARY.md

<interfaces>
<!-- From Plan 03 -->
From src/hooks/use-goals.ts:
```typescript
export function useGoals(): {
  saveUpGoals: SaveUpGoal[];
  payDownGoals: PayDownGoal[];
  goalBudgetCategories: GoalBudgetCategory[];
  refreshKey: number;
  addSaveUpGoal: (goal: Omit<SaveUpGoal, "id">) => SaveUpGoal;
  updateSaveUpGoal: (id: string, updates: Partial<SaveUpGoal>) => void;
  deleteSaveUpGoal: (id: string) => void;
  addPayDownGoal: (goal: Omit<PayDownGoal, "id">) => PayDownGoal;
  updatePayDownGoal: (id: string, updates: Partial<PayDownGoal>) => void;
  deletePayDownGoal: (id: string) => void;
};
```

From src/services/goal-storage/types.ts:
```typescript
export interface GoalBudgetCategory {
  id: string; name: string; targetAmount: number; activity: number; groupId: string;
}
```

<!-- Existing dashboard screen structure -->
From app/(tabs)/index.tsx:
```typescript
// Dashboard has: BalanceCard, MetricsCard, recent transactions section
// Uses ScrollView, SectionHeader helper, Card component
// Pattern for new cards: add after MetricsCard section
```

<!-- Existing settings screen structure -->
From app/(tabs)/settings.tsx:
```typescript
// Uses SettingRow helper component: { icon, label, value?, onPress? }
// Uses SectionLabel for section headers
// Pattern: add new SettingRow in relevant section
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GoalDashboardCard and navigation integration across screens</name>
  <files>src/components/goals/GoalDashboardCard.tsx, app/(tabs)/index.tsx, app/(tabs)/settings.tsx, app/(tabs)/transactions.tsx, app/(tabs)/budget.tsx</files>
  <read_first>
    - app/(tabs)/index.tsx (full file -- dashboard structure for insertion point)
    - app/(tabs)/settings.tsx (full file -- settings rows for insertion point)
    - app/(tabs)/transactions.tsx (full file -- transactions screen for import button)
    - app/(tabs)/budget.tsx (full file -- budget view for goal category integration)
    - src/hooks/use-goals.ts (useGoals hook API)
    - src/components/ui/Card.tsx (Card component)
    - .planning/phases/06-goals-statement-import/06-UI-SPEC.md (GoalDashboardCard layout)
  </read_first>
  <action>
    1. Create src/components/goals/GoalDashboardCard.tsx (per D-14):
       - Uses useGoals hook to get saveUpGoals and payDownGoals
       - Shows top 2-3 goals (save-up first, then pay-down) with mini progress bars
       - Card header: "Goals" title (goals.title) + "See All" link (goals.seeAll)
       - For save-up goals: name + mini GoalProgress bar + percentage
       - For pay-down goals: name + "X left" formatted balance
       - Pressable wrapper navigates to /goals via useRouter
       - "See All" also navigates to /goals
       - If no goals exist, show a minimal prompt: "Set a savings goal" with + icon
       - Card default variant styling

    2. Modify app/(tabs)/index.tsx:
       - Import GoalDashboardCard from src/components/goals/GoalDashboardCard
       - Add GoalDashboardCard after the MetricsCard section (before recent transactions)
       - Wrap in View with mb-4 spacing
       - No other changes to existing dashboard functionality

    3. Modify app/(tabs)/settings.tsx:
       - Import useRouter from expo-router (if not already imported)
       - Add a new SettingRow in the appropriate section:
         icon: "📥", label: t("import.title") or "Import Statement", onPress: () => router.push("/import")
       - Place it in the data/account management section (near export/backup if exists, otherwise after general settings)

    4. Modify app/(tabs)/transactions.tsx:
       - Add an "Import" button in the header area or as a secondary action
       - Pressable with import icon (Upload from lucide-react-native or text "Import")
       - onPress: router.push("/import")
       - Place near the FAB or in the header right side
       - Style: subtle, not competing with the main add transaction FAB

    5. Modify app/(tabs)/budget.tsx (per D-13, GOAL-05):
       - Import useGoals from src/hooks/use-goals
       - Get goalBudgetCategories from useGoals hook
       - Add goal categories to the budget display:
         If budget view has a categories list, append goalBudgetCategories as a "Goals" group
         Show each goal category with name ("Goal: [name]"), target amount, and activity (contributions)
       - If budget view uses mock data, inject goalBudgetCategories into the mock data structure
       - Mark goal categories visually (e.g., Target icon from lucide, or teal accent)
  </action>
  <verify>
    <automated>cd /home/siam/Personal/DoinikHishab && npx jest --no-coverage --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - GoalDashboardCard renders top 2-3 goals with progress indicators
    - Dashboard screen shows GoalDashboardCard between MetricsCard and recent transactions
    - Settings screen has "Import Statement" row that navigates to /import
    - Transactions screen has "Import" button that navigates to /import
    - Budget view shows goal contribution categories from useGoals hook
    - All navigation works: dashboard card -> goals, settings row -> import, transactions button -> import
    - Existing screen functionality is preserved (no regressions)
  </acceptance_criteria>
  <done>Goals and import features are wired into the main app navigation with dashboard card, settings link, transactions button, and budget integration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Goals and Import features</name>
  <files>none</files>
  <action>
    Present the user with verification steps for the complete Phase 6 implementation.
    Start the dev server if not running: `bun run start`
    Guide the user through testing both goals and import flows on device/emulator.
  </action>
  <what-built>
    Complete Phase 6 goals and import features:
    1. Save-up goals with progress tracking, status badges, and monthly contribution calculation
    2. Pay-down goals with amortization schedules and avalanche vs snowball strategy comparison
    3. Goal budget categories appearing in budget view
    4. bKash/Nagad statement import with file picking, parsing, duplicate detection, and type mapping
    5. Navigation integration: dashboard Goals card, settings Import link, transactions Import button
  </what-built>
  <how-to-verify>
    Start the app: `bun run start` and open on device/emulator

    Goals flow:
    1. From dashboard, tap "Goals" card -> goals screen opens
    2. Tap "Create Goal" -> bottom sheet form opens
    3. Fill in name, target amount, target date, linked account -> save
    4. New goal card appears with progress bar and status badge
    5. Tap goal card -> detail screen with full info
    6. Go back, tap "Add Debt" -> fill debt form -> save
    7. Add a second debt -> strategy comparison section appears
    8. Tap "Show schedule" on a debt -> amortization table expands
    9. Check budget tab -> "Goal: [name]" categories appear

    Import flow:
    10. From settings, tap "Import Statement" -> import screen opens
    11. Tap file picker -> native file dialog opens (filter PDF/XLS/TXT)
    12. Select a test file -> parsing spinner -> review list appears
    13. Duplicate transactions have yellow "Duplicate" badges
    14. Tap type pill to toggle expense/income
    15. Tap "Import N Transactions" -> success toast appears

    Verify:
    - All text appears in correct language (English/Bengali based on locale)
    - Progress bars animate smoothly
    - Amounts display in BDT format
    - Navigation works in all directions
  </how-to-verify>
  <verify>
    <automated>cd /home/siam/Personal/DoinikHishab && npx jest --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All goals flows work end-to-end (create, view, edit, delete goals and debts)
    - Strategy comparison visible with 2+ debts
    - Import flow works from file selection to confirmed import
    - Navigation integration works from all entry points
    - User approves the complete phase
  </acceptance_criteria>
  <done>Phase 6 is complete -- goals tracking, debt payoff strategies, and statement import are all functional and approved by the user.</done>
  <resume-signal>Type "approved" if everything works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Dashboard shows Goals card with working navigation
- Settings has Import Statement link with working navigation
- Budget view includes goal contribution categories
- Full test suite passes: `npx jest --no-coverage`
- Human verification confirms all flows work end-to-end
</verification>

<success_criteria>
- GoalDashboardCard created and integrated into dashboard
- Navigation links added to settings and transactions screens
- Budget integration shows goal categories
- Human approval of complete phase functionality
</success_criteria>

<output>
After completion, create `.planning/phases/06-goals-statement-import/06-05-SUMMARY.md`
</output>
