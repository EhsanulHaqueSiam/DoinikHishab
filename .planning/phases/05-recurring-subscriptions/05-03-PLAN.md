---
phase: 05-recurring-subscriptions
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/recurring/SubscriptionCard.tsx
  - src/components/recurring/SubscriptionCard.test.tsx
  - src/components/recurring/SubscriptionList.tsx
  - src/components/recurring/SubscriptionList.test.tsx
  - src/components/recurring/SubscriptionHeader.tsx
  - src/components/recurring/SubscriptionHeader.test.tsx
  - src/components/recurring/AddSubscriptionForm.tsx
autonomous: true
requirements:
  - RECR-03
  - RECR-04

must_haves:
  truths:
    - "User sees detected subscription suggestions with Confirm/Dismiss actions"
    - "User can view confirmed subscriptions with total monthly and annual burn rate"
    - "User can swipe left to remove a subscription"
    - "User can manually add a subscription via bottom sheet form"
    - "Burn rate header shows total monthly and annual cost"
  artifacts:
    - path: "src/components/recurring/SubscriptionCard.tsx"
      provides: "Suggestion card for detected subscriptions with Confirm/Not a subscription actions"
      min_lines: 40
    - path: "src/components/recurring/SubscriptionList.tsx"
      provides: "Confirmed subscription list with swipe-to-delete"
      min_lines: 60
    - path: "src/components/recurring/SubscriptionHeader.tsx"
      provides: "Monthly + annual burn rate display"
      min_lines: 30
    - path: "src/components/recurring/AddSubscriptionForm.tsx"
      provides: "Bottom sheet form for manual subscription creation"
      min_lines: 50
  key_links:
    - from: "src/components/recurring/SubscriptionCard.tsx"
      to: "src/components/recurring/recurring-types.ts"
      via: "imports DetectedSubscription type"
      pattern: "import.*DetectedSubscription.*from.*recurring-types"
    - from: "src/components/recurring/SubscriptionList.tsx"
      to: "react-native-gesture-handler/ReanimatedSwipeable"
      via: "uses Swipeable for swipe-to-delete"
      pattern: "import.*ReanimatedSwipeable"
    - from: "src/components/recurring/SubscriptionHeader.tsx"
      to: "@lib/currency"
      via: "formats burn rate amounts"
      pattern: "import.*formatCurrency.*from.*currency"
    - from: "src/components/recurring/AddSubscriptionForm.tsx"
      to: "@gorhom/bottom-sheet"
      via: "renders as bottom sheet form"
      pattern: "import.*BottomSheet.*from.*@gorhom/bottom-sheet"
---

<objective>
Build the subscription management UI components: detected subscription suggestion cards, confirmed subscription list with swipe-to-delete, burn rate header, and manual add form.

Purpose: Delivers RECR-03 (subscription auto-detection UI) and RECR-04 (subscription management view with burn rate). These provide the subscription tracking interfaces.
Output: 4 components ready to be assembled into the recurring screen in Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-recurring-subscriptions/05-CONTEXT.md
@.planning/phases/05-recurring-subscriptions/05-RESEARCH.md
@.planning/phases/05-recurring-subscriptions/05-UI-SPEC.md
@.planning/phases/05-recurring-subscriptions/05-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 -->

From src/components/recurring/recurring-types.ts:
```typescript
export type RecurringFrequency = "weekly" | "monthly" | "yearly";

export interface Subscription {
  id: string;
  payee: string;
  amount: number;          // paisa
  frequency: RecurringFrequency;
  categoryId: string;
  nextDueDate: string;     // YYYY-MM-DD
  isActive: boolean;
  type: "income" | "expense";
}

export interface DetectedSubscription {
  payee: string;
  amount: number;          // paisa (average)
  frequency: RecurringFrequency;
  confidence: number;      // 0-1
  occurrences: number;
  lastDate: string;
  categoryId?: string;
}
```

From src/services/recurring-storage/index.ts:
```typescript
export function getSubscriptions(): Subscription[];
export function saveSubscription(sub: Omit<Subscription, "id">): Subscription;
export function removeSubscription(id: string): void;
export function getDismissedPayees(): string[];
export function dismissPayee(payee: string): void;
```

From src/hooks/use-recurring-data.ts:
```typescript
export function useRecurringData(): {
  bills: BillItem[];
  subscriptions: Subscription[];
  detectedSubscriptions: DetectedSubscription[];
  isLoading: boolean;
};
```

Swipeable pattern (from 05-RESEARCH.md):
```typescript
import Swipeable from "react-native-gesture-handler/ReanimatedSwipeable";
// renderRightActions returns delete button
// onSwipeableOpen triggers removal
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SubscriptionCard and SubscriptionHeader components</name>
  <files>
    src/components/recurring/SubscriptionCard.tsx
    src/components/recurring/SubscriptionCard.test.tsx
    src/components/recurring/SubscriptionHeader.tsx
    src/components/recurring/SubscriptionHeader.test.tsx
  </files>
  <read_first>
    src/components/recurring/recurring-types.ts
    src/services/recurring-storage/index.ts
    src/components/dashboard/BalanceCard.tsx
    src/components/ui/Card.tsx
    src/lib/currency.ts
    .planning/phases/05-recurring-subscriptions/05-UI-SPEC.md
    .planning/phases/05-recurring-subscriptions/05-CONTEXT.md
    jest.setup.js
  </read_first>
  <action>
    1. Create `src/components/recurring/SubscriptionCard.tsx` (per D-06):
       - Props: `{ subscription: DetectedSubscription; onConfirm: (sub: DetectedSubscription) => void; onDismiss: (payee: string) => void }`
       - Card layout: `mx-4 mt-3 rounded-2xl p-4 bg-surface-200 border border-accent/20`
       - Content:
         - Payee name: `text-sm font-bold text-foreground`
         - Detection info row: frequency label (`t("recurring.monthly")` etc), amount `formatCurrency(sub.amount)`, occurrences count
         - Confidence indicator: if confidence > 0.8, show with `text-primary-500` ("High match"), else `text-surface-800` ("Possible match")
         - Two action buttons in a `flex-row gap-3 mt-3`:
           - "Confirm" button: `bg-primary-500 rounded-xl px-4 py-2`, text `text-white font-bold text-sm` -- calls `onConfirm(subscription)`
           - "Not a subscription" button: `bg-transparent rounded-xl px-4 py-2 border border-surface-600`, text `text-surface-900 font-bold text-sm` -- calls `onDismiss(subscription.payee)`
       - Accessibility: buttons have accessibilityRole="button", card has accessibilityLabel describing the detected subscription

    2. Create `src/components/recurring/SubscriptionCard.test.tsx`:
       - Test: renders payee name and amount
       - Test: calls onConfirm when Confirm button pressed
       - Test: calls onDismiss when "Not a subscription" button pressed
       - Test: shows confidence level text

    3. Create `src/components/recurring/SubscriptionHeader.tsx` (per D-07):
       - Props: `{ subscriptions: Subscription[] }`
       - Calculates burn rate from subscriptions:
         - Monthly total: sum all monthly amounts + (weekly amounts * 4.33) + (yearly amounts / 12). All in paisa, integer math
         - Annual total: monthly total * 12
       - Layout following BalanceCard pattern: `mx-4 mt-4 rounded-2xl p-5 bg-surface-200 border border-primary-400/15` with `shadow("#0d9488", 0, 8, 0.12, 24, 8)`
       - Top: label `t("recurring.burnRate")` in `text-2xs font-semibold text-surface-900 uppercase tracking-widest`
       - Monthly total: `formatCurrency(monthlyTotal)` in `text-hero font-bold text-foreground`
       - Divider: `h-px bg-border/30 my-4`
       - Bottom row: left column "Monthly" label + value in text-primary-700, right column "Annual" label + value in text-surface-900
       - Accessibility: `accessibilityLabel` on the total with "Monthly subscription total: {amount}"

    4. Create `src/components/recurring/SubscriptionHeader.test.tsx`:
       - Test: renders monthly and annual burn rate amounts
       - Test: correctly calculates burn rate from mixed frequency subscriptions
       - Test: renders zero when no subscriptions
  </action>
  <verify>
    <automated>bun run test -- --testPathPattern="(SubscriptionCard|SubscriptionHeader)" --no-coverage -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function SubscriptionCard" src/components/recurring/SubscriptionCard.tsx
    - grep -q "onConfirm" src/components/recurring/SubscriptionCard.tsx
    - grep -q "onDismiss" src/components/recurring/SubscriptionCard.tsx
    - grep -q "formatCurrency" src/components/recurring/SubscriptionCard.tsx
    - grep -q "export function SubscriptionHeader" src/components/recurring/SubscriptionHeader.tsx
    - grep -q "burnRate" src/components/recurring/SubscriptionHeader.tsx
    - grep -q "shadow" src/components/recurring/SubscriptionHeader.tsx
    - grep -q "accessibilityLabel" src/components/recurring/SubscriptionHeader.tsx
  </acceptance_criteria>
  <done>
    - SubscriptionCard shows detected subscription with payee, amount, frequency, and confidence level
    - SubscriptionCard has Confirm and "Not a subscription" buttons that trigger callbacks per D-06
    - SubscriptionHeader calculates and displays monthly and annual burn rate per D-07
    - SubscriptionHeader follows BalanceCard visual pattern
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: SubscriptionList with swipe-to-delete and AddSubscriptionForm</name>
  <files>
    src/components/recurring/SubscriptionList.tsx
    src/components/recurring/SubscriptionList.test.tsx
    src/components/recurring/AddSubscriptionForm.tsx
  </files>
  <read_first>
    src/components/recurring/recurring-types.ts
    src/components/recurring/SubscriptionCard.tsx
    src/components/recurring/SubscriptionHeader.tsx
    src/services/recurring-storage/index.ts
    src/components/ui/Input.tsx
    src/components/ui/Button.tsx
    src/lib/currency.ts
    .planning/phases/05-recurring-subscriptions/05-UI-SPEC.md
    .planning/phases/05-recurring-subscriptions/05-CONTEXT.md
    .planning/phases/05-recurring-subscriptions/05-RESEARCH.md
    jest.setup.js
  </read_first>
  <action>
    1. Create `src/components/recurring/SubscriptionList.tsx` (per D-07):
       - Props: `{ subscriptions: Subscription[]; onRemove: (id: string) => void }`
       - Uses FlashList from `@shopify/flash-list` with `estimatedItemSize={56}`
       - Each row wrapped in `Swipeable` from `react-native-gesture-handler/ReanimatedSwipeable`:
         - `renderRightActions`: returns a View with `bg-danger justify-center items-center px-6` containing trash icon (Lucide `Trash2`, size 20, color white) and `t("recurring.delete")` text in white
         - `onSwipeableOpen={(direction) => { if (direction === "right") onRemove(sub.id) }}` -- per D-07 swipe left reveals right action
         - `overshootRight={false}` to prevent over-swipe
       - Row content: `flex-row items-center px-4 py-3 bg-surface-100`
         - Left: category icon placeholder (View with `w-8 h-8 rounded-full bg-surface-300 items-center justify-center`) -- use first letter of payee as fallback
         - Center (flex-1, ml-3): payee name (`text-sm font-bold text-foreground`), frequency label below (`text-2xs text-surface-800`, e.g., "Monthly")
         - Right: amount (`text-sm font-bold text-surface-900`, `formatCurrency(sub.amount)`)
       - Accessibility: each row has accessibilityActions with `{ name: "delete", label: t("recurring.delete") }` as alternative to swipe gesture (per UI Spec accessibility contract)
       - Empty state: if subscriptions is empty, show centered `t("recurring.noSubscriptionsTitle")` and `t("recurring.noSubscriptionsBody")`

    2. Create `src/components/recurring/SubscriptionList.test.tsx`:
       - Test: renders subscription payee names and amounts
       - Test: renders frequency labels for each subscription
       - Test: shows empty state when no subscriptions
       - Test: calls onRemove when accessibility delete action triggered
       - Mock Swipeable as identity wrapper (using the jest.setup.js mock from Plan 01)

    3. Create `src/components/recurring/AddSubscriptionForm.tsx`:
       - Props: `{ sheetRef: React.RefObject<BottomSheet>; onSave: (sub: Omit<Subscription, "id">) => void }`
       - Uses `@gorhom/bottom-sheet` with snapPoints `["70%"]`
       - Form fields:
         - Payee name: Input component (required, validation: non-empty)
         - Amount: Input component with numeric keyboard (`keyboardType="numeric"`), label `t("recurring.amount")`
         - Frequency: 3-pill toggle (Weekly / Monthly / Yearly) using ViewToggle-style pills. Default: "monthly"
         - Type: 2-pill toggle (Income / Expense). Default: "expense"
       - Category: simple text input for categoryId (placeholder "mock_cat_1"), or hardcode to "mock_cat_1" for now since Convex is offline
       - Next due date: simple text input accepting YYYY-MM-DD format, default to first of next month
       - Save button: `bg-primary-500 rounded-xl py-3 mt-4`, text `text-white font-bold text-center`, label `t("recurring.save")`
       - Cancel button: ghost style, `t("recurring.cancel")`
       - On save: validate payee is non-empty and amount > 0, call `onSave({ payee, amount: amountInPaisa, frequency, categoryId, nextDueDate, isActive: true, type })`, close sheet
       - Haptic feedback on save via `expo-haptics` `notificationAsync(NotificationFeedbackType.Success)`
  </action>
  <verify>
    <automated>bun run test -- --testPathPattern="SubscriptionList" --no-coverage -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function SubscriptionList" src/components/recurring/SubscriptionList.tsx
    - grep -q "Swipeable" src/components/recurring/SubscriptionList.tsx
    - grep -q "FlashList" src/components/recurring/SubscriptionList.tsx
    - grep -q "onRemove" src/components/recurring/SubscriptionList.tsx
    - grep -q "accessibilityActions" src/components/recurring/SubscriptionList.tsx
    - grep -q "noSubscriptionsTitle" src/components/recurring/SubscriptionList.tsx
    - grep -q "export function AddSubscriptionForm" src/components/recurring/AddSubscriptionForm.tsx
    - grep -q "BottomSheet" src/components/recurring/AddSubscriptionForm.tsx
    - grep -q "onSave" src/components/recurring/AddSubscriptionForm.tsx
    - grep -q "Haptics" src/components/recurring/AddSubscriptionForm.tsx
  </acceptance_criteria>
  <done>
    - SubscriptionList renders confirmed subscriptions with payee, amount, frequency per D-07
    - Swipe-to-delete reveals red delete action per D-07
    - Accessibility delete action available as alternative to swipe
    - Empty state shown when no subscriptions
    - AddSubscriptionForm collects payee, amount, frequency, type, next due date
    - Form validates required fields before saving
    - Haptic feedback on successful save
    - Tests pass
  </done>
</task>

</tasks>

<verification>
bun run test -- --testPathPattern="(SubscriptionCard|SubscriptionHeader|SubscriptionList)" --no-coverage
</verification>

<success_criteria>
- SubscriptionCard shows detected subscriptions with Confirm/Dismiss actions per D-06
- SubscriptionList renders confirmed subscriptions with swipe-to-delete per D-07
- SubscriptionHeader shows monthly + annual burn rate per D-07
- AddSubscriptionForm provides manual subscription creation per D-07
- All status indicators use correct semantic colors
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-recurring-subscriptions/05-03-SUMMARY.md`
</output>
