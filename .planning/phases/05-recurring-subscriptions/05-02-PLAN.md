---
phase: 05-recurring-subscriptions
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/recurring/BillSummaryCard.tsx
  - src/components/recurring/ViewToggle.tsx
  - src/components/recurring/BillCalendarGrid.tsx
  - src/components/recurring/BillCalendarGrid.test.tsx
  - src/components/recurring/BillDaySheet.tsx
  - src/components/recurring/BillListView.tsx
  - src/components/recurring/BillListView.test.tsx
autonomous: true
requirements:
  - RECR-01
  - RECR-02

must_haves:
  truths:
    - "User sees a calendar grid with colored dots indicating paid (teal), upcoming (saffron), and overdue (red) bills"
    - "User can toggle between calendar grid and chronological list view"
    - "User can tap a calendar day to see bill details in a bottom sheet"
    - "List view groups bills by This Week / Next Week / This Month with status badges"
    - "Summary card shows total upcoming and total paid amounts"
  artifacts:
    - path: "src/components/recurring/BillCalendarGrid.tsx"
      provides: "7-column calendar grid with bill status dots and month navigation"
      min_lines: 80
    - path: "src/components/recurring/BillListView.tsx"
      provides: "Grouped bill list with status badges"
      min_lines: 60
    - path: "src/components/recurring/BillSummaryCard.tsx"
      provides: "Monthly bill summary card following BalanceCard pattern"
      min_lines: 30
    - path: "src/components/recurring/ViewToggle.tsx"
      provides: "Calendar/List pill toggle"
      min_lines: 20
    - path: "src/components/recurring/BillDaySheet.tsx"
      provides: "Bottom sheet showing bills for a selected day"
      min_lines: 40
  key_links:
    - from: "src/components/recurring/BillCalendarGrid.tsx"
      to: "src/components/recurring/recurring-types.ts"
      via: "imports BillItem type"
      pattern: "import.*BillItem.*from.*recurring-types"
    - from: "src/components/recurring/BillCalendarGrid.tsx"
      to: "src/components/recurring/BillDaySheet.tsx"
      via: "opens bottom sheet on day tap"
      pattern: "BillDaySheet"
    - from: "src/components/recurring/BillSummaryCard.tsx"
      to: "@lib/currency"
      via: "formats amounts with formatCurrency"
      pattern: "import.*formatCurrency.*from.*currency"
---

<objective>
Build the calendar and bill view UI components: summary card, calendar grid with colored status dots, list view with grouped sections, view toggle, and day detail bottom sheet.

Purpose: Delivers RECR-01 (calendar view with status indicators) and RECR-02 (toggle between calendar and list views with monthly totals). These are the primary bill tracking interfaces.
Output: 5 components ready to be assembled into the recurring screen in Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-recurring-subscriptions/05-CONTEXT.md
@.planning/phases/05-recurring-subscriptions/05-RESEARCH.md
@.planning/phases/05-recurring-subscriptions/05-UI-SPEC.md
@.planning/phases/05-recurring-subscriptions/05-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that these components consume -->

From src/components/recurring/recurring-types.ts (created in Plan 01):
```typescript
export type RecurringFrequency = "weekly" | "monthly" | "yearly";
export type BillStatus = "paid" | "upcoming" | "overdue";

export interface BillItem {
  id: string;
  subscriptionId: string;
  payee: string;
  amount: number;       // paisa
  dueDate: string;      // YYYY-MM-DD
  status: BillStatus;
  categoryId: string;
  type: "income" | "expense";
}
```

From src/hooks/use-recurring-data.ts (created in Plan 01):
```typescript
export function useRecurringData(): {
  bills: BillItem[];
  subscriptions: Subscription[];
  detectedSubscriptions: DetectedSubscription[];
  isLoading: boolean;
};
```

From src/components/dashboard/BalanceCard.tsx (pattern reference):
```typescript
// Card pattern: mx-4 mt-4 rounded-2xl p-5 bg-surface-200 border border-primary-400/15
// with shadow("#0d9488", 0, 8, 0.12, 24, 8)
// Labels: text-2xs font-semibold text-surface-900 uppercase tracking-widest
// Values: text-hero font-bold text-foreground mt-1 tracking-tight
```

From src/components/reports/TimeRangeSelector.tsx (pill toggle pattern):
```typescript
// Pill pattern: rounded-full px-3 py-2 min-w-[44px] items-center
// Active: bg-primary-500, text-white
// Inactive: bg-surface-300, text-surface-900
```

From src/components/reports/ChartTooltip.tsx:
```typescript
export function ChartTooltip({ label, amount, percentage, isPrivacy, onDismiss }: ChartTooltipProps);
```

From jest.setup.js (bottom-sheet mock added in Plan 01):
```javascript
jest.mock("@gorhom/bottom-sheet", () => { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BillSummaryCard, ViewToggle, and BillCalendarGrid with month navigation</name>
  <files>
    src/components/recurring/BillSummaryCard.tsx
    src/components/recurring/ViewToggle.tsx
    src/components/recurring/BillCalendarGrid.tsx
    src/components/recurring/BillCalendarGrid.test.tsx
    src/components/recurring/BillDaySheet.tsx
  </files>
  <read_first>
    src/components/recurring/recurring-types.ts
    src/components/dashboard/BalanceCard.tsx
    src/components/reports/TimeRangeSelector.tsx
    src/components/reports/ChartTooltip.tsx
    src/lib/currency.ts
    src/lib/platform.ts
    .planning/phases/05-recurring-subscriptions/05-UI-SPEC.md
    .planning/phases/05-recurring-subscriptions/05-CONTEXT.md
    jest.setup.js
  </read_first>
  <action>
    1. Create `src/components/recurring/BillSummaryCard.tsx` (per D-04):
       - Props: `{ upcomingTotal: number; paidTotal: number; billCount: number }` (amounts in paisa)
       - Follow BalanceCard pattern exactly: `mx-4 mt-4 rounded-2xl p-5 bg-surface-200 border border-primary-400/15` with `shadow("#0d9488", 0, 8, 0.12, 24, 8)`
       - Top section: label "Upcoming Bills" (`t("recurring.upcomingBills")`), value `formatCurrency(upcomingTotal)` in text-hero
       - Divider: `h-px bg-border/30 my-4`
       - Bottom section: two columns -- left shows "Paid" label with `formatCurrency(paidTotal)` in text-primary-700, right shows "Bills" label with billCount in text-surface-900
       - Import `shadow` from `@lib/platform`, `formatCurrency` from lib path, `useTranslation` from react-i18next

    2. Create `src/components/recurring/ViewToggle.tsx` (per RECR-02):
       - Props: `{ selected: "calendar" | "list"; onChange: (view: "calendar" | "list") => void }`
       - Two pills following TimeRangeSelector pattern: rounded-full px-3 py-2 min-w-[44px]
       - Active: bg-primary-500, text-white. Inactive: bg-surface-300, text-surface-900
       - Labels: `t("recurring.calendarView")` and `t("recurring.listView")`
       - Accessibility: accessibilityRole="tab", accessibilityState={{ selected }}

    3. Create `src/components/recurring/BillCalendarGrid.tsx` (per D-01, D-02):
       - Props: `{ bills: BillItem[]; onDayPress: (date: string, dayBills: BillItem[]) => void }`
       - Internal state: `currentMonth` (Date) for month navigation
       - Month navigation: left/right chevron buttons (`ChevronLeft`, `ChevronRight` from lucide-react-native) with month/year label
       - 7-column grid using nested View rows. Weekday headers: Sun Mon Tue Wed Thu Fri Sat (short locale-appropriate abbreviations)
       - Day cells: Pressable with minimum 44px touch target (accessibility). Show day number. Below the number, show colored dots:
         - teal dot (bg-primary-500) if any bill has status "paid"
         - saffron dot (bg-accent) if any bill has status "upcoming"
         - red dot (bg-danger) if any bill has status "overdue"
       - Today's cell: `bg-primary-400/20 border border-primary-500/30`, day number in `text-primary-700 font-bold`
       - Days from adjacent months: `opacity-30`
       - Tapping a day with bills calls `onDayPress(dateString, billsForThatDay)`
       - Tapping a day without bills: no action
       - Month navigation: simple `setCurrentMonth` state update (no animation needed for v1 per Claude's discretion)
       - Use Lucide `ChevronLeft` and `ChevronRight` for nav arrows (size 20, color text-surface-900)

    4. Create `src/components/recurring/BillDaySheet.tsx`:
       - Props: `{ bills: BillItem[]; date: string; sheetRef: React.RefObject<BottomSheet> }`
       - Uses `@gorhom/bottom-sheet` with snapPoints `["40%"]`
       - Header: formatted date (e.g., "March 28, 2026")
       - List of bills for that day, each showing:
         - Payee name (text-sm font-bold text-foreground)
         - Amount: `formatCurrency(bill.amount)` (text-sm text-surface-900)
         - Status badge using NativeWind classes:
           - paid: `bg-primary-500/20 text-primary-500` with text "Paid"
           - upcoming: `bg-accent/20 text-accent` with text "Upcoming"
           - overdue: `bg-danger/20 text-danger` with text "Overdue"
       - Import BottomSheet from `@gorhom/bottom-sheet`, BottomSheetView for content

    5. Create `src/components/recurring/BillCalendarGrid.test.tsx`:
       - Test: renders 7 weekday headers
       - Test: renders day numbers for current month
       - Test: shows colored dot for day with a bill
       - Test: calls onDayPress when tapping a day with bills
       - Test: does NOT call onDayPress when tapping a day without bills
       - Mock bills data: use 2-3 BillItem objects with known dates and statuses
  </action>
  <verify>
    <automated>bun run test -- --testPathPattern="BillCalendarGrid" --no-coverage -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function BillSummaryCard" src/components/recurring/BillSummaryCard.tsx
    - grep -q "formatCurrency" src/components/recurring/BillSummaryCard.tsx
    - grep -q "shadow" src/components/recurring/BillSummaryCard.tsx
    - grep -q "export function ViewToggle" src/components/recurring/ViewToggle.tsx
    - grep -q "accessibilityRole" src/components/recurring/ViewToggle.tsx
    - grep -q "export function BillCalendarGrid" src/components/recurring/BillCalendarGrid.tsx
    - grep -q "onDayPress" src/components/recurring/BillCalendarGrid.tsx
    - grep -q "bg-primary-500" src/components/recurring/BillCalendarGrid.tsx
    - grep -q "bg-accent" src/components/recurring/BillCalendarGrid.tsx
    - grep -q "bg-danger" src/components/recurring/BillCalendarGrid.tsx
    - grep -q "export function BillDaySheet" src/components/recurring/BillDaySheet.tsx
    - grep -q "BottomSheet" src/components/recurring/BillDaySheet.tsx
  </acceptance_criteria>
  <done>
    - BillSummaryCard renders total upcoming, paid amounts, and bill count following BalanceCard visual pattern
    - ViewToggle provides Calendar/List pill toggle with accessibility roles
    - BillCalendarGrid renders 7-column grid with colored status dots (teal/saffron/red per D-02)
    - BillCalendarGrid supports month navigation and today highlighting
    - BillDaySheet opens as bottom sheet showing bills for a tapped day
    - CalendarGrid tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: BillListView with grouped sections and status badges</name>
  <files>
    src/components/recurring/BillListView.tsx
    src/components/recurring/BillListView.test.tsx
  </files>
  <read_first>
    src/components/recurring/recurring-types.ts
    src/components/recurring/BillSummaryCard.tsx
    src/lib/currency.ts
    .planning/phases/05-recurring-subscriptions/05-UI-SPEC.md
    .planning/phases/05-recurring-subscriptions/05-CONTEXT.md
  </read_first>
  <action>
    1. Create `src/components/recurring/BillListView.tsx` (per D-03):
       - Props: `{ bills: BillItem[] }`
       - Groups bills chronologically into sections: "This Week", "Next Week", "This Month" (per D-03)
       - Grouping logic:
         - Get current date, calculate end of this week (Saturday), end of next week
         - Bills with dueDate within this week -> "This Week" (`t("recurring.thisWeek")`)
         - Bills with dueDate within next week -> "Next Week" (`t("recurring.nextWeek")`)
         - Bills with dueDate in rest of current month -> "This Month" (`t("recurring.thisMonth")`)
         - Only show sections that have bills
       - Use FlashList from `@shopify/flash-list` for the list (with `estimatedItemSize={60}`)
       - Section headers: `text-2xs font-semibold text-surface-800 uppercase tracking-widest` with `px-4 py-2 mt-3`
       - Each bill row: View with `px-4 py-3 flex-row items-center justify-between`
         - Left side: payee name (`text-sm font-bold text-foreground`), due date below (`text-2xs text-surface-800`)
         - Right side: amount (`text-sm font-bold text-surface-900`, `formatCurrency(bill.amount)`), status badge
       - Status badges (inline rounded pills):
         - paid: `bg-primary-500/20 px-2 py-0.5 rounded-full`, text `text-2xs font-bold text-primary-500`, label "Paid"
         - upcoming: `bg-accent/20 px-2 py-0.5 rounded-full`, text `text-2xs font-bold text-accent`, label "Upcoming"
         - overdue: `bg-danger/20 px-2 py-0.5 rounded-full`, text `text-2xs font-bold text-danger`, label "Overdue"
       - Accessibility: each badge has `accessibilityLabel` with status text (per UI Spec accessibility contract)
       - Empty state: if no bills, show centered text `t("recurring.noBillsTitle")` and `t("recurring.noBillsBody")`

    2. Create `src/components/recurring/BillListView.test.tsx`:
       - Test: renders section headers for groups that have bills
       - Test: renders bill payee names and amounts
       - Test: renders correct status badge for each bill status
       - Test: shows empty state when no bills provided
       - Test: groups bills correctly by date proximity
       - Use mock BillItem data with dates spread across this week, next week, and this month
  </action>
  <verify>
    <automated>bun run test -- --testPathPattern="BillListView" --no-coverage -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function BillListView" src/components/recurring/BillListView.tsx
    - grep -q "FlashList" src/components/recurring/BillListView.tsx
    - grep -q "thisWeek" src/components/recurring/BillListView.tsx
    - grep -q "nextWeek" src/components/recurring/BillListView.tsx
    - grep -q "thisMonth" src/components/recurring/BillListView.tsx
    - grep -q "bg-primary-500/20" src/components/recurring/BillListView.tsx
    - grep -q "bg-accent/20" src/components/recurring/BillListView.tsx
    - grep -q "bg-danger/20" src/components/recurring/BillListView.tsx
    - grep -q "noBillsTitle" src/components/recurring/BillListView.tsx
    - grep -q "accessibilityLabel" src/components/recurring/BillListView.tsx
  </acceptance_criteria>
  <done>
    - BillListView renders bills grouped by "This Week" / "Next Week" / "This Month" per D-03
    - Each row shows payee, amount, due date, and color-coded status badge per D-02
    - Status badges use correct semantic colors: teal=paid, saffron=upcoming, red=overdue
    - Empty state shown when no bills
    - Uses FlashList for performance
    - Tests pass
  </done>
</task>

</tasks>

<verification>
bun run test -- --testPathPattern="(BillCalendarGrid|BillListView)" --no-coverage
</verification>

<success_criteria>
- BillSummaryCard follows BalanceCard visual pattern with upcoming/paid totals
- ViewToggle provides Calendar/List switching with accessibility
- BillCalendarGrid renders 7-column grid with teal/saffron/red dots per D-02
- BillCalendarGrid supports month navigation
- BillDaySheet shows bill details in bottom sheet
- BillListView groups bills by This Week/Next Week/This Month per D-03
- All status badges use correct semantic colors
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-recurring-subscriptions/05-02-SUMMARY.md`
</output>
