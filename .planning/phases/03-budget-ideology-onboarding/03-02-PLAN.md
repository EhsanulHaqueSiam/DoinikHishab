---
phase: 03-budget-ideology-onboarding
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/budget/ReadyToAssignHero.tsx
  - src/components/budget/ReadyToAssignHero.test.tsx
  - src/components/budget/SinkingFundSection.tsx
  - src/components/budget/SinkingFundRow.tsx
  - src/components/budget/RuleTip.tsx
  - src/components/budget/RuleTip.test.tsx
  - app/(tabs)/budget.tsx
autonomous: true
requirements: [BUDG-01, BUDG-02, BUDG-03, ONBD-03]

must_haves:
  truths:
    - "Ready to Assign displays as a hero banner with text-hero font, teal glow when positive, red glow when negative"
    - "Ready to Assign shows 'Give every taka a job' subtitle (translated) when positive, 'All money assigned' when zero, warning when negative"
    - "Sinking funds section appears above regular category groups with 'True Expenses' header"
    - "Each sinking fund row shows progress bar with teal fill on-track and saffron fill behind"
    - "Each sinking fund row shows 'Suggested: X/month' text below progress bar"
    - "RuleTip cards appear below relevant budget rows and can be dismissed permanently"
    - "Dismissed tips persist to MMKV and do not reappear"
  artifacts:
    - path: "src/components/budget/ReadyToAssignHero.tsx"
      provides: "Hero banner component for Ready to Assign"
      exports: ["ReadyToAssignHero"]
    - path: "src/components/budget/ReadyToAssignHero.test.tsx"
      provides: "Render tests for ReadyToAssignHero component"
      contains: "ReadyToAssignHero"
    - path: "src/components/budget/SinkingFundSection.tsx"
      provides: "Container for sinking fund rows with True Expenses header"
      exports: ["SinkingFundSection"]
    - path: "src/components/budget/SinkingFundRow.tsx"
      provides: "Individual sinking fund progress row"
      exports: ["SinkingFundRow"]
    - path: "src/components/budget/RuleTip.tsx"
      provides: "Dismissible contextual YNAB rule tip card"
      exports: ["RuleTip"]
    - path: "src/components/budget/RuleTip.test.tsx"
      provides: "Render tests for RuleTip component"
      contains: "RuleTip"
    - path: "app/(tabs)/budget.tsx"
      provides: "Budget screen with hero banner, sinking funds, and rule tips integrated"
      contains: "ReadyToAssignHero"
  key_links:
    - from: "src/components/budget/ReadyToAssignHero.tsx"
      to: "src/lib/i18n/en.json"
      via: "useTranslation for readyToAssign.positive, readyToAssign.overAssigned, readyToAssign.zero"
      pattern: "t\\(.*readyToAssign"
    - from: "src/components/budget/SinkingFundRow.tsx"
      to: "src/services/budget-engine/index.ts"
      via: "uses calculateSinkingFundSuggest for auto-suggest amount"
      pattern: "calculateSinkingFundSuggest"
    - from: "src/components/budget/RuleTip.tsx"
      to: "src/services/onboarding/index.ts"
      via: "isTipDismissed and dismissTip for MMKV persistence"
      pattern: "isTipDismissed|dismissTip"
    - from: "app/(tabs)/budget.tsx"
      to: "src/components/budget/ReadyToAssignHero.tsx"
      via: "imports and renders ReadyToAssignHero"
      pattern: "import.*ReadyToAssignHero"
---

<objective>
Build the budget screen UI upgrades: Ready to Assign hero banner, True Expenses sinking fund section with progress bars and auto-suggest, and contextual YNAB rule tips. This transforms the budget screen from a basic spreadsheet into the ideological core of the app.

Purpose: The budget screen is where YNAB philosophy lives. The hero banner makes "give every taka a job" the most prominent element. Sinking funds teach users to plan for irregular expenses. Rule tips educate in context.

Output: 4 new components + 2 component test files + budget screen integration, all rendering with mock data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-budget-ideology-onboarding/03-CONTEXT.md
@.planning/phases/03-budget-ideology-onboarding/03-UI-SPEC.md
@.planning/phases/03-budget-ideology-onboarding/03-01-SUMMARY.md

@app/(tabs)/budget.tsx
@src/components/budget/GoalProgress.tsx
@src/components/budget/BudgetRow.tsx
@src/components/dashboard/BalanceCard.tsx
@src/services/budget-engine/index.ts
@src/services/mock-data/index.ts
@src/services/onboarding/index.ts
@src/lib/currency.ts
@src/lib/platform.ts

<interfaces>
From src/services/budget-engine/index.ts (after Plan 01):
```typescript
export function calculateSinkingFundSuggest(
  targetAmount: number, accumulated: number, monthsRemaining: number
): number;
```

From src/services/onboarding/index.ts (after Plan 01):
```typescript
export function isTipDismissed(ruleId: string): boolean;
export function dismissTip(ruleId: string): void;
```

From src/services/mock-data/index.ts (after Plan 01):
```typescript
export interface SinkingFundTemplate {
  id: string; nameKey: string; icon: string; color: string;
  defaultTargetPaisa: number; defaultMonths: number; preSelected: boolean;
}
export const SINKING_FUND_TEMPLATES: readonly SinkingFundTemplate[];
```

From src/lib/currency.ts:
```typescript
export function formatCurrency(amountPaisa: number): string;
```

From src/lib/platform.ts:
```typescript
export function shadow(color: string, offsetX: number, offsetY: number, opacity: number, radius: number, elevation?: number): object;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReadyToAssignHero + SinkingFundSection + SinkingFundRow + RuleTip components with render tests</name>
  <files>
    src/components/budget/ReadyToAssignHero.tsx,
    src/components/budget/ReadyToAssignHero.test.tsx,
    src/components/budget/SinkingFundSection.tsx,
    src/components/budget/SinkingFundRow.tsx,
    src/components/budget/RuleTip.tsx,
    src/components/budget/RuleTip.test.tsx
  </files>
  <read_first>
    app/(tabs)/budget.tsx,
    src/components/budget/GoalProgress.tsx,
    src/components/dashboard/BalanceCard.tsx,
    src/services/budget-engine/index.ts,
    src/services/mock-data/index.ts,
    src/services/onboarding/index.ts,
    src/lib/currency.ts,
    src/lib/platform.ts,
    .planning/phases/03-budget-ideology-onboarding/03-UI-SPEC.md
  </read_first>
  <action>
    **1. Create src/components/budget/ReadyToAssignHero.tsx** (per D-07):

    Props: `{ amount: number; onPress?: () => void }`

    Visual contract from UI-SPEC:
    - Full-width banner wrapped in Pressable with `active:bg-surface-400/30`
    - Container: `bg-surface-200 rounded-2xl p-5 mx-4 mt-3 items-center` with `border border-primary-400/15`
    - Amount in `text-hero font-bold tracking-tight` with lineHeight: 42 (same as BalanceCard)
    - Three states (use ternary with STATIC class strings, never dynamic construction):
      - Positive (amount > 0): amount class `text-primary-700`, glow via `shadow("#0d9488", 0, 4, 0.2, 20, 8)`, subtitle: `t("readyToAssign.positive")`
      - Zero (amount === 0): amount class `text-primary-700`, no glow, subtitle: `t("readyToAssign.zero")`
      - Over-assigned (amount < 0): amount class `text-danger`, glow via `shadow("#f87171", 0, 4, 0.15, 20, 8)`, subtitle: `t("readyToAssign.overAssigned")`
    - Label: `text-2xs font-bold text-surface-800 uppercase tracking-widest` "READY TO ASSIGN" from `t("readyToAssign.title")`
    - Subtitle: `text-2xs text-surface-800 mt-1`
    - Accessibility: `accessibilityRole="button"` with `accessibilityLabel={t("readyToAssign.title") + ": " + formattedAmount}`
    - Import `formatCurrency` from `@lib/currency`, `shadow` from `@lib/platform`, `useTranslation` from `react-i18next`

    **2. Create src/components/budget/ReadyToAssignHero.test.tsx** — Render tests:

    ```typescript
    import { render, screen } from "@testing-library/react-native";
    import { ReadyToAssignHero } from "./ReadyToAssignHero";

    describe("ReadyToAssignHero", () => {
      it("renders positive amount with teal styling", () => {
        render(<ReadyToAssignHero amount={500000} />);
        // Verify the formatted amount text is present
        expect(screen.getByText(/5,000/)).toBeTruthy();
        // Verify the positive subtitle
        expect(screen.getByText(/readyToAssign.positive/)).toBeTruthy();
      });

      it("renders zero amount with 'all assigned' message", () => {
        render(<ReadyToAssignHero amount={0} />);
        expect(screen.getByText(/readyToAssign.zero/)).toBeTruthy();
      });

      it("renders negative amount with warning styling", () => {
        render(<ReadyToAssignHero amount={-200000} />);
        expect(screen.getByText(/readyToAssign.overAssigned/)).toBeTruthy();
      });

      it("has accessible button role", () => {
        render(<ReadyToAssignHero amount={500000} />);
        expect(screen.getByRole("button")).toBeTruthy();
      });
    });
    ```

    Adjust test expectations to match actual i18n mock setup (if i18n mock returns keys as-is, match keys; if it returns translations, match English text).

    **3. Create src/components/budget/SinkingFundRow.tsx** (per D-02, D-03):

    Props: `{ template: SinkingFundTemplate; accumulated: number; targetAmount: number; monthsRemaining: number; onPress?: () => void }`

    Visual contract from UI-SPEC:
    - Full row width Pressable with `active:bg-surface-400/30`, min 56px height
    - Left: Lucide icon (20px) + fund name (from t(template.nameKey)) in `text-sm font-bold text-foreground`, `numberOfLines={1} ellipsizeMode="tail"`
    - Center: Progress bar — 8px height (`h-2`), `rounded-full`, background `bg-surface-400`
      - Calculate progress: `Math.min(accumulated / targetAmount, 1)` (guard targetAmount === 0)
      - On track (progress >= expected month fraction): fill `bg-primary-500` (teal)
      - Behind: fill `bg-accent` (saffron)
      - Funded (progress >= 1): fill `bg-primary-500` (teal), 100% width
      - Use Reanimated `withTiming` 600ms easeOut for progress bar width animation
    - Right: percentage in `text-2xs font-bold` + amount `text-2xs text-surface-800` (format: "X of Y")
    - Below bar: "Suggested: X/month" text in `text-2xs text-surface-800`
      - Calculate suggest via `calculateSinkingFundSuggest(targetAmount, accumulated, monthsRemaining)`
      - Display: `t("sinkingFunds.suggested", { amount: formatCurrency(suggestAmount) })`
    - Status badge: "On Track" / "Behind" / "Funded" in `text-2xs font-bold` with appropriate color class
    - Accessibility: `accessibilityLabel={fundName + ", " + percentage + "% " + t("sinkingFunds.ofTarget") + ", " + statusLabel}`

    **4. Create src/components/budget/SinkingFundSection.tsx** (per D-04):

    Props: `{ sinkingFunds: SinkingFundTemplate[] }` (or no props — read templates internally)

    Visual contract from UI-SPEC:
    - Container with "True Expenses" label: `text-2xs font-bold uppercase tracking-widest text-surface-800 px-4 mb-2`
    - Background: `bg-surface-200 rounded-2xl mx-4 mt-3 p-3 overflow-hidden`
    - Renders SINKING_FUND_TEMPLATES (pre-selected ones only) as SinkingFundRow list
    - Mock data for accumulated amounts: generate deterministic mock accumulation (e.g., Eid Fund 60% funded, School Fees 30%, Wedding Gifts 80%, Medical Reserve 20%)
    - Mock monthsRemaining: derive from current date to typical target dates
    - Empty state: when no sinking funds, show "No True Expenses yet" message per UI-SPEC empty state
    - Separator between rows: `h-px bg-border/15 mx-2`

    **5. Create src/components/budget/RuleTip.tsx** (per D-08, ONBD-03):

    Props: `{ ruleId: string; titleKey: string; bodyKey: string }`

    Visual contract from UI-SPEC:
    - Check `isTipDismissed(ruleId)` from onboarding service — if true, return null
    - Container: `mx-4 my-2 p-3 bg-primary-50 border border-primary-400/20 rounded-xl`
    - Title: `text-xs font-bold text-primary-700`
    - Body: `text-2xs text-surface-900 leading-4`
    - Dismiss button: `text-2xs text-surface-800` "Got it" / `t("tips.dismiss")` aligned right, `hitSlop={8}`
    - On dismiss: call `dismissTip(ruleId)`, animate collapse with Reanimated `withTiming` 200ms height to 0 + opacity to 0
    - Use `useCallback` for dismiss handler
    - Local state `[dismissed, setDismissed]` to trigger re-render on dismiss (MMKV read is synchronous but component needs to re-render)
    - Accessibility: dismiss button has `accessibilityRole="button"` with `accessibilityLabel={t("tips.dismiss")}`

    **6. Create src/components/budget/RuleTip.test.tsx** — Render tests:

    ```typescript
    import { render, screen, fireEvent } from "@testing-library/react-native";
    import { RuleTip } from "./RuleTip";

    // Mock onboarding service
    jest.mock("../../services/onboarding", () => ({
      isTipDismissed: jest.fn().mockReturnValue(false),
      dismissTip: jest.fn(),
    }));

    describe("RuleTip", () => {
      it("renders tip title and body when not dismissed", () => {
        render(<RuleTip ruleId="rule_1" titleKey="tips.rule1Title" bodyKey="tips.rule1Body" />);
        expect(screen.getByText(/rule1Title/)).toBeTruthy();
        expect(screen.getByText(/rule1Body/)).toBeTruthy();
      });

      it("renders dismiss button with correct label", () => {
        render(<RuleTip ruleId="rule_1" titleKey="tips.rule1Title" bodyKey="tips.rule1Body" />);
        expect(screen.getByText(/dismiss/i)).toBeTruthy();
      });

      it("returns null when tip is already dismissed", () => {
        const { isTipDismissed } = require("../../services/onboarding");
        isTipDismissed.mockReturnValueOnce(true);
        const { toJSON } = render(<RuleTip ruleId="rule_1" titleKey="tips.rule1Title" bodyKey="tips.rule1Body" />);
        expect(toJSON()).toBeNull();
      });
    });
    ```

    Adjust test expectations to match actual i18n mock setup in the project's jest.setup.js.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/DoinikHishab && bun run test -- ReadyToAssignHero RuleTip --no-coverage 2>&1 | tail -15 && echo "---" && grep -c "export function ReadyToAssignHero" src/components/budget/ReadyToAssignHero.tsx && grep -c "export function SinkingFundRow" src/components/budget/SinkingFundRow.tsx && grep -c "export function SinkingFundSection" src/components/budget/SinkingFundSection.tsx && grep -c "export function RuleTip" src/components/budget/RuleTip.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "text-hero" src/components/budget/ReadyToAssignHero.tsx` finds hero typography class
    - `grep "text-primary-700" src/components/budget/ReadyToAssignHero.tsx` finds teal positive color
    - `grep "text-danger" src/components/budget/ReadyToAssignHero.tsx` finds red over-assigned color
    - `grep "readyToAssign.positive" src/components/budget/ReadyToAssignHero.tsx` finds i18n key usage
    - `grep "calculateSinkingFundSuggest" src/components/budget/SinkingFundRow.tsx` finds auto-suggest integration
    - `grep "bg-primary-500" src/components/budget/SinkingFundRow.tsx` finds teal progress bar fill
    - `grep "bg-accent" src/components/budget/SinkingFundRow.tsx` finds saffron behind-schedule fill
    - `grep "True Expenses" src/components/budget/SinkingFundSection.tsx` OR `grep "sinkingFunds.title" src/components/budget/SinkingFundSection.tsx` finds header
    - `grep "isTipDismissed" src/components/budget/RuleTip.tsx` finds MMKV check
    - `grep "dismissTip" src/components/budget/RuleTip.tsx` finds dismiss persistence
    - `bun run test -- ReadyToAssignHero` passes with 0 failures
    - `bun run test -- RuleTip` passes with 0 failures
  </acceptance_criteria>
  <done>
    4 new budget components created with 2 companion test files: ReadyToAssignHero with teal/red states, hero typography, and render tests for 3 states + accessibility. SinkingFundRow with animated progress bar and auto-suggest. SinkingFundSection with True Expenses header and mock data. RuleTip with dismissible MMKV-persisted contextual cards and render tests for show/hide/dismiss behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate components into budget screen</name>
  <files>
    app/(tabs)/budget.tsx
  </files>
  <read_first>
    app/(tabs)/budget.tsx,
    src/components/budget/ReadyToAssignHero.tsx,
    src/components/budget/SinkingFundSection.tsx,
    src/components/budget/RuleTip.tsx,
    .planning/phases/03-budget-ideology-onboarding/03-UI-SPEC.md
  </read_first>
  <action>
    Modify `app/(tabs)/budget.tsx` to integrate the new components per the visual hierarchy in UI-SPEC:

    **1. Replace existing Ready to Assign card** (lines ~70-108 in current budget.tsx) with `<ReadyToAssignHero>`:
    - Import `ReadyToAssignHero` from `@components/budget/ReadyToAssignHero`
    - Replace the existing `<Pressable>` + `<Card>` Ready to Assign block with:
      ```tsx
      <ReadyToAssignHero amount={readyToAssign} onPress={() => { /* open assign modal or navigate */ }} />
      ```

    **2. Add SinkingFundSection below ReadyToAssignHero, above the ScrollView budget grid**:
    - Import `SinkingFundSection` from `@components/budget/SinkingFundSection`
    - Place `<SinkingFundSection />` directly after `<ReadyToAssignHero />`, before the Column Headers
    - The section is self-contained (loads its own mock data)

    **3. Add RuleTip for Rule 2 below SinkingFundSection**:
    - Import `RuleTip` from `@components/budget/RuleTip`
    - After `<SinkingFundSection />`, render:
      ```tsx
      <RuleTip ruleId="rule_2" titleKey="tips.rule2Title" bodyKey="tips.rule2Body" />
      ```
    - This shows the "Embrace True Expenses" tip below sinking funds (per D-08)

    **4. Add RuleTip for Rule 3 below first overspent category group**:
    - Inside the groupedBudgets map, after rendering rows for a group that has overspent categories (any budget.available < 0), render:
      ```tsx
      {group.budgets.some((b: any) => b.available < 0) && (
        <RuleTip ruleId="rule_3" titleKey="tips.rule3Title" bodyKey="tips.rule3Body" />
      )}
      ```
    - This shows "Roll with the Punches" tip contextually below overspent categories (per D-08)

    **5. Add RuleTip for Rule 1 below ReadyToAssignHero when readyToAssign > 0**:
    - After `<ReadyToAssignHero>` and before `<SinkingFundSection>`:
      ```tsx
      {readyToAssign > 0 && (
        <RuleTip ruleId="rule_1" titleKey="tips.rule1Title" bodyKey="tips.rule1Body" />
      )}
      ```

    **6. Keep existing budget grid and AssignMoney modal unchanged**. Only add new components; do not rewrite working code.

    **Visual hierarchy order (top to bottom per UI-SPEC):**
    1. Month Navigator (unchanged)
    2. ReadyToAssignHero (NEW — replaces old card)
    3. RuleTip Rule 1 (conditional, when RTA > 0)
    4. SinkingFundSection (NEW)
    5. RuleTip Rule 2 (below sinking funds)
    6. Column Headers (unchanged)
    7. Category Groups + BudgetRows (unchanged, with Rule 3 tips)
  </action>
  <verify>
    <automated>cd /home/siam/Personal/DoinikHishab && grep -c "ReadyToAssignHero" app/\(tabs\)/budget.tsx && grep -c "SinkingFundSection" app/\(tabs\)/budget.tsx && grep -c "RuleTip" app/\(tabs\)/budget.tsx && bun run test --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import.*ReadyToAssignHero" app/\(tabs\)/budget.tsx` finds the import
    - `grep "import.*SinkingFundSection" app/\(tabs\)/budget.tsx` finds the import
    - `grep "import.*RuleTip" app/\(tabs\)/budget.tsx` finds the import
    - `grep "<ReadyToAssignHero" app/\(tabs\)/budget.tsx` finds component usage
    - `grep "<SinkingFundSection" app/\(tabs\)/budget.tsx` finds component usage
    - `grep "ruleId.*rule_2" app/\(tabs\)/budget.tsx` finds Rule 2 tip
    - `grep "ruleId.*rule_3" app/\(tabs\)/budget.tsx` finds Rule 3 tip
    - Old Ready to Assign Card/Pressable block is removed (no `text-2xl font-bold` for RTA amount — now `text-hero`)
    - `bun run test` passes (no regressions)
  </acceptance_criteria>
  <done>
    Budget screen renders ReadyToAssignHero as the most prominent element with hero typography and ideology subtitle. SinkingFundSection appears above category groups with progress bars for 4 Bengali-named sinking funds. Contextual RuleTips appear below relevant sections (Rule 1 when money to assign, Rule 2 below sinking funds, Rule 3 below overspent categories). All tips dismissible with MMKV persistence.
  </done>
</task>

</tasks>

<verification>
1. Budget screen renders without crashes: `bun run web` and navigate to Budget tab
2. ReadyToAssignHero shows teal glow when positive, red when negative, correct subtitles
3. SinkingFundSection shows 4 sinking funds with progress bars
4. RuleTips appear and can be dismissed (do not reappear after dismiss)
5. `bun run test -- ReadyToAssignHero RuleTip` — component tests pass
6. `bun run test` — full suite passes
7. `bun run lint` — no new Biome errors
</verification>

<success_criteria>
- Budget screen has ReadyToAssignHero with text-hero font and teal/red glow states
- SinkingFundSection renders above category groups with "True Expenses" header
- Each sinking fund shows teal/saffron progress bar + "Suggested: X/month" auto-suggest
- 3 contextual rule tips render at appropriate positions and dismiss permanently
- ReadyToAssignHero and RuleTip have passing render tests
- All text uses i18n t() for Bengali/English support
- No regressions in existing budget functionality
</success_criteria>

<output>
After completion, create `.planning/phases/03-budget-ideology-onboarding/03-02-SUMMARY.md`
</output>
