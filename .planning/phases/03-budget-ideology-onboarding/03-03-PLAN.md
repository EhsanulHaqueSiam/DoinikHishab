---
phase: 03-budget-ideology-onboarding
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/dashboard/MetricsCard.tsx
  - src/components/dashboard/MetricsCard.test.tsx
  - src/components/onboarding/RuleCarousel.tsx
  - src/components/onboarding/RuleCarousel.test.tsx
  - src/components/onboarding/RuleCard.tsx
  - src/components/onboarding/StepIndicator.tsx
  - src/components/onboarding/CategoryTemplateSelector.tsx
  - app/(tabs)/index.tsx
  - app/(tabs)/settings.tsx
autonomous: true
requirements: [BUDG-04, BUDG-05, ONBD-01, ONBD-04]

must_haves:
  truths:
    - "Age of Money displays on dashboard with trend arrow (green up for improving, red down for declining)"
    - "Days of Buffering displays next to Age of Money with configurable lookback period"
    - "MetricsCard shows placeholder text when no data available"
    - "Settings gear on MetricsCard opens bottom sheet for lookback period selection"
    - "RuleCarousel swipes horizontally through 4 YNAB rule cards with pagination dots"
    - "StepIndicator shows 5 dots with active/completed/inactive states"
    - "CategoryTemplateSelector shows 2x2 grid of 4 Bangladeshi template sets"
    - "Settings screen has 'Redo Setup' and 'Buffer Lookback' rows"
  artifacts:
    - path: "src/components/dashboard/MetricsCard.tsx"
      provides: "Age of Money + Days of Buffering card"
      exports: ["MetricsCard"]
    - path: "src/components/dashboard/MetricsCard.test.tsx"
      provides: "Render tests for MetricsCard component"
      contains: "MetricsCard"
    - path: "src/components/onboarding/RuleCarousel.tsx"
      provides: "Horizontal ScrollView carousel for YNAB 4 Rules"
      exports: ["RuleCarousel"]
    - path: "src/components/onboarding/RuleCarousel.test.tsx"
      provides: "Render tests for RuleCarousel component"
      contains: "RuleCarousel"
    - path: "src/components/onboarding/RuleCard.tsx"
      provides: "Single rule card with icon, title, description, example"
      exports: ["RuleCard"]
    - path: "src/components/onboarding/StepIndicator.tsx"
      provides: "Horizontal stepper dots for onboarding progress"
      exports: ["StepIndicator"]
    - path: "src/components/onboarding/CategoryTemplateSelector.tsx"
      provides: "2x2 grid of category template selection cards"
      exports: ["CategoryTemplateSelector"]
    - path: "app/(tabs)/index.tsx"
      provides: "Dashboard with MetricsCard below BalanceCard"
      contains: "MetricsCard"
    - path: "app/(tabs)/settings.tsx"
      provides: "Settings with Redo Setup and Buffer Lookback rows"
      contains: "redoSetup"
  key_links:
    - from: "src/components/dashboard/MetricsCard.tsx"
      to: "src/hooks/use-metrics.ts"
      via: "imports useMetrics for ageOfMoney, daysOfBuffering"
      pattern: "import.*useMetrics"
    - from: "src/components/onboarding/RuleCarousel.tsx"
      to: "src/services/onboarding/rules.ts"
      via: "imports YNAB_RULES constant"
      pattern: "import.*YNAB_RULES"
    - from: "src/components/onboarding/CategoryTemplateSelector.tsx"
      to: "src/services/mock-data/index.ts"
      via: "imports CATEGORY_TEMPLATE_SETS"
      pattern: "import.*CATEGORY_TEMPLATE_SETS"
    - from: "app/(tabs)/index.tsx"
      to: "src/components/dashboard/MetricsCard.tsx"
      via: "renders MetricsCard below BalanceCard"
      pattern: "<MetricsCard"
---

<objective>
Build the dashboard MetricsCard (Age of Money + Days of Buffering), all onboarding UI components (RuleCarousel, RuleCard, StepIndicator, CategoryTemplateSelector), and integrate into dashboard and settings screens.

Purpose: The MetricsCard gives users financial health feedback on the dashboard. The onboarding components will be composed into the guided flow screens in Plan 04. Building them here allows parallel work with Plan 02's budget screen changes.

Output: 5 new components + 2 component test files, dashboard integration, settings integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-budget-ideology-onboarding/03-CONTEXT.md
@.planning/phases/03-budget-ideology-onboarding/03-UI-SPEC.md
@.planning/phases/03-budget-ideology-onboarding/03-01-SUMMARY.md

@app/(tabs)/index.tsx
@app/(tabs)/settings.tsx
@src/components/dashboard/BalanceCard.tsx
@src/hooks/use-metrics.ts
@src/hooks/use-onboarding.ts
@src/services/onboarding/rules.ts
@src/services/onboarding/index.ts
@src/services/mock-data/index.ts
@src/lib/platform.ts

<interfaces>
From src/hooks/use-metrics.ts (after Plan 01):
```typescript
export interface MetricsData {
  ageOfMoney: number | null;
  ageOfMoneyTrend: "improving" | "declining" | "flat" | null;
  daysOfBuffering: number | null;
  lookbackDays: number;
}
export function useMetrics(): MetricsData;
```

From src/services/onboarding/rules.ts (after Plan 01):
```typescript
export interface YnabRule {
  id: number; titleKey: string; descKey: string; exampleKey: string;
  icon: string; color: string;
}
export const YNAB_RULES: YnabRule[];
```

From src/services/onboarding/index.ts (after Plan 01):
```typescript
export function getLookbackDays(): number;
export function setLookbackDays(days: number): void;
```

From src/services/mock-data/index.ts (after Plan 01):
```typescript
export interface CategoryTemplateSet {
  id: string; nameKey: string; icon: string;
  descriptionKey: string; categoryIds: string[];
}
export const CATEGORY_TEMPLATE_SETS: readonly CategoryTemplateSet[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MetricsCard + onboarding UI components with render tests (MetricsCard, RuleCarousel)</name>
  <files>
    src/components/dashboard/MetricsCard.tsx,
    src/components/dashboard/MetricsCard.test.tsx,
    src/components/onboarding/RuleCarousel.tsx,
    src/components/onboarding/RuleCarousel.test.tsx,
    src/components/onboarding/RuleCard.tsx,
    src/components/onboarding/StepIndicator.tsx,
    src/components/onboarding/CategoryTemplateSelector.tsx
  </files>
  <read_first>
    src/components/dashboard/BalanceCard.tsx,
    src/hooks/use-metrics.ts,
    src/services/onboarding/rules.ts,
    src/services/onboarding/index.ts,
    src/services/mock-data/index.ts,
    src/lib/platform.ts,
    .planning/phases/03-budget-ideology-onboarding/03-UI-SPEC.md
  </read_first>
  <action>
    **1. Create src/components/dashboard/MetricsCard.tsx** (per D-05, D-06):

    Uses `useMetrics()` hook. Two-column card layout with vertical divider.

    Visual contract from UI-SPEC:
    - Card wrapper: `bg-surface-200 border border-primary-400/15 rounded-2xl p-4 mx-4 mt-3`
    - Apply `shadow("#0d9488", 0, 4, 0.08, 16, 4)` for subtle teal shadow
    - Two columns via `flex-row` with `flex-1` each, vertical divider `w-px bg-border/30` between

    Left column (Age of Money):
    - Label: "AGE OF MONEY" in `text-2xs font-bold text-surface-800 uppercase tracking-widest`
    - Value: `{ageOfMoney} days` or "-- days" if null, in `text-lg font-bold text-foreground mt-0.5`
    - Trend arrow: Use Lucide icons based on `ageOfMoneyTrend`:
      - "improving": `TrendingUp` icon in `color="#34d399"` (success green)
      - "declining": `TrendingDown` icon in `color="#f87171"` (danger red)
      - "flat": `Minus` icon in `color` surface-800
      - null: no icon
    - Trend icon size: 14px, inline with value text
    - When null: show `t("metrics.noAgeDesc")` in `text-2xs text-surface-700 mt-1`
    - Accessibility: `accessibilityLabel={t("metrics.ageOfMoney") + ": " + (ageOfMoney ? ageOfMoney + " " + t("metrics.days") : t("metrics.noDays")) + ", " + trendLabel}`

    Right column (Days of Buffering):
    - Label: "BUFFER" in `text-2xs font-bold text-surface-800 uppercase tracking-widest`
    - Value: `{daysOfBuffering} days` or "-- days" if null, in `text-lg font-bold text-foreground mt-0.5`
    - Settings gear: Lucide `Settings` icon (12px) with `hitSlop={8}` for 28px tap area, `active:opacity-60`
    - Gear accessibility: `accessibilityRole="button"` with `accessibilityLabel={t("metrics.lookbackSettings")}`
    - When null: show `t("metrics.noBufferDesc")` in `text-2xs text-surface-700 mt-1`
    - Gear onPress: accept `onSettingsPress` prop (parent will handle bottom sheet)

    **2. Create src/components/dashboard/MetricsCard.test.tsx** — Render tests:

    ```typescript
    import { render, screen } from "@testing-library/react-native";
    import { MetricsCard } from "./MetricsCard";

    // Mock useMetrics hook
    jest.mock("../../hooks/use-metrics", () => ({
      useMetrics: jest.fn(),
    }));

    import { useMetrics } from "../../hooks/use-metrics";

    describe("MetricsCard", () => {
      it("renders Age of Money value when data exists", () => {
        (useMetrics as jest.Mock).mockReturnValue({
          ageOfMoney: 25, ageOfMoneyTrend: "improving",
          daysOfBuffering: 45, lookbackDays: 90,
        });
        render(<MetricsCard />);
        expect(screen.getByText(/25/)).toBeTruthy();
        expect(screen.getByText(/45/)).toBeTruthy();
      });

      it("renders placeholder text when data is null", () => {
        (useMetrics as jest.Mock).mockReturnValue({
          ageOfMoney: null, ageOfMoneyTrend: null,
          daysOfBuffering: null, lookbackDays: 90,
        });
        render(<MetricsCard />);
        expect(screen.getByText(/--/)).toBeTruthy();
      });

      it("renders settings gear button", () => {
        (useMetrics as jest.Mock).mockReturnValue({
          ageOfMoney: 25, ageOfMoneyTrend: "improving",
          daysOfBuffering: 45, lookbackDays: 90,
        });
        const onSettingsPress = jest.fn();
        render(<MetricsCard onSettingsPress={onSettingsPress} />);
        // Verify settings gear is accessible
        expect(screen.getByLabelText(/lookback/i)).toBeTruthy();
      });
    });
    ```

    Adjust test expectations to match actual i18n mock setup.

    **3. Create src/components/onboarding/RuleCard.tsx** (per D-09):

    Props: `{ rule: YnabRule; width: number }`

    Visual contract from UI-SPEC:
    - Card: `bg-surface-200 rounded-2xl p-6` with `width` style prop
    - Horizontal margin: `mx-4` (16px each side)
    - Lucide icon at top: 48px, tinted with `rule.color`
    - Rule title: `text-lg font-bold text-foreground mt-4`
    - Rule description: `text-sm text-surface-900 mt-2 leading-5`
    - Example box: `mt-4 pl-3 border-l-2` with `borderLeftColor: "#e6a444"` (saffron accent)
      - Example text: `text-sm text-surface-800 leading-5`
    - All text from `t(rule.titleKey)`, `t(rule.descKey)`, `t(rule.exampleKey)`
    - Import Lucide icons dynamically by name — use a small icon map for the 4 specific icons needed: `Briefcase`, `Calendar`, `RefreshCw`, `Clock`

    **4. Create src/components/onboarding/RuleCarousel.tsx** (per D-09):

    Visual contract from UI-SPEC:
    - Horizontal ScrollView with `pagingEnabled`, `snapToInterval={CARD_WIDTH}`, `decelerationRate="fast"`
    - `CARD_WIDTH = Math.min(screenWidth - 32, 448)` to respect 480px WebContainer
    - `showsHorizontalScrollIndicator={false}`
    - `scrollEventThrottle={16}`
    - Renders `YNAB_RULES.map()` as `<RuleCard>` components
    - Below ScrollView: `PaginationDots` — a row of 4 dots
    - Dots use Reanimated `useSharedValue` + `useAnimatedStyle`:
      - Active dot: 24px width, 12px height, `bg-primary-500`, `rounded-full`
      - Inactive dot: 12px diameter, `bg-surface-500`, `rounded-full`
      - Width interpolation: 200ms `withTiming`
    - `onScroll` event calculates `activeIndex = Math.round(offsetX / CARD_WIDTH)`
    - Pagination dots centered horizontally with `mt-4`
    - No autoplay
    - Accessibility: `accessibilityRole="adjustable"` on ScrollView

    Props: `{ onComplete?: () => void }` — called when user swipes past last card

    **5. Create src/components/onboarding/RuleCarousel.test.tsx** — Render tests:

    ```typescript
    import { render, screen } from "@testing-library/react-native";
    import { RuleCarousel } from "./RuleCarousel";

    describe("RuleCarousel", () => {
      it("renders all 4 YNAB rule cards", () => {
        render(<RuleCarousel />);
        // Verify 4 rule cards are rendered (check for rule title i18n keys)
        expect(screen.getByText(/rule1\.title/)).toBeTruthy();
        expect(screen.getByText(/rule2\.title/)).toBeTruthy();
        expect(screen.getByText(/rule3\.title/)).toBeTruthy();
        expect(screen.getByText(/rule4\.title/)).toBeTruthy();
      });

      it("renders pagination dots", () => {
        const { toJSON } = render(<RuleCarousel />);
        const json = JSON.stringify(toJSON());
        // Verify pagination dots container exists (4 dots rendered)
        expect(json).toBeTruthy();
      });
    });
    ```

    Adjust test expectations to match actual i18n mock and Reanimated mock setup.

    **6. Create src/components/onboarding/StepIndicator.tsx** (per D-10, ONBD-04):

    Props: `{ currentStep: number; completedSteps: boolean[]; onStepPress?: (step: number) => void }`

    Visual contract from UI-SPEC:
    - Horizontal row of 5 dots centered, `py-4`
    - Gap between dots: 12px (`gap-3`)
    - Three states:
      - Active (index === currentStep): 24px width, 12px height pill, `bg-primary-500 rounded-full`
      - Completed (completedSteps[index] === true): 12px diameter circle, `bg-primary-700 rounded-full`, tiny checkmark Lucide `Check` icon (8px) inside
      - Not reached: 12px diameter circle, `bg-surface-500 rounded-full`
    - Completed dots tappable: `onStepPress(step)` navigates back
    - Each dot: Pressable with `hitSlop={12}` for 36px tap area
    - Active dot: Reanimated `withSpring` scale pulse 1.0 -> 1.2 -> 1.0 (300ms, damping 12) on step change
    - Cannot tap future incomplete dots (no forward skip via dots)
    - Accessibility: `accessibilityLabel={stepLabel + (isComplete ? ", completed" : isActive ? ", current" : "")}`
    - Step labels from i18n: `t("onboarding.step1")` through `t("onboarding.step5")`

    **7. Create src/components/onboarding/CategoryTemplateSelector.tsx** (per D-11):

    Props: `{ selectedId: string | null; onSelect: (templateId: string) => void }`

    Visual contract from UI-SPEC:
    - 2x2 grid via `flex-row flex-wrap` with each card taking ~48% width
    - Gap: 12px between cards (`gap-3`)
    - Each card: Pressable, `rounded-xl p-4`
      - Selected: `border-2 border-primary-500 bg-primary-50`
      - Unselected: `border border-border/40 bg-card`
    - Lucide icon (32px) at top, tinted with template-specific color
    - Template name: `text-sm font-bold text-foreground mt-2`
    - Description: `text-2xs text-surface-800 mt-1`
    - Text from `t(template.nameKey)` and `t(template.descriptionKey)`
    - Import `CATEGORY_TEMPLATE_SETS` from mock-data
    - Icon map for 4 icons: `BookOpen`, `Briefcase`, `Laptop`, `Users`
  </action>
  <verify>
    <automated>cd /home/siam/Personal/DoinikHishab && bun run test -- MetricsCard RuleCarousel --no-coverage 2>&1 | tail -15 && echo "---" && for f in src/components/dashboard/MetricsCard.tsx src/components/onboarding/RuleCarousel.tsx src/components/onboarding/RuleCard.tsx src/components/onboarding/StepIndicator.tsx src/components/onboarding/CategoryTemplateSelector.tsx; do echo "$f: $(test -f $f && echo EXISTS || echo MISSING)"; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep "useMetrics" src/components/dashboard/MetricsCard.tsx` finds hook import
    - `grep "TrendingUp\|trending-up" src/components/dashboard/MetricsCard.tsx` finds trend icon
    - `grep "AGE OF MONEY\|ageLabel\|metrics.ageLabel" src/components/dashboard/MetricsCard.tsx` finds label
    - `grep "YNAB_RULES" src/components/onboarding/RuleCarousel.tsx` finds rules import
    - `grep "pagingEnabled\|snapToInterval" src/components/onboarding/RuleCarousel.tsx` finds carousel behavior
    - `grep "currentStep\|completedSteps" src/components/onboarding/StepIndicator.tsx` finds step tracking props
    - `grep "CATEGORY_TEMPLATE_SETS" src/components/onboarding/CategoryTemplateSelector.tsx` finds template data
    - All 5 component files + 2 test files exist and export named components
    - `bun run test -- MetricsCard` passes with 0 failures
    - `bun run test -- RuleCarousel` passes with 0 failures
  </acceptance_criteria>
  <done>
    MetricsCard renders Age of Money with trend arrow and Days of Buffering with settings gear, with passing render tests for data/null/settings states. RuleCarousel swipes through 4 YNAB rule cards with animated pagination dots, with passing render test verifying all 4 rules render. StepIndicator shows 5 dots with active/completed/inactive states and spring animation. CategoryTemplateSelector shows 2x2 grid of Student/Professional/Freelancer/Family templates with selection state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate MetricsCard into dashboard + settings screen updates</name>
  <files>
    app/(tabs)/index.tsx,
    app/(tabs)/settings.tsx
  </files>
  <read_first>
    app/(tabs)/index.tsx,
    app/(tabs)/settings.tsx,
    src/components/dashboard/MetricsCard.tsx,
    src/hooks/use-onboarding.ts,
    src/services/onboarding/index.ts,
    .planning/phases/03-budget-ideology-onboarding/03-UI-SPEC.md
  </read_first>
  <action>
    **1. Modify app/(tabs)/index.tsx** — Add MetricsCard below BalanceCard (per D-05, D-06):

    - Import `MetricsCard` from `@components/dashboard/MetricsCard`
    - Import `BottomSheet` from `@gorhom/bottom-sheet` and related (BottomSheetView, useRef)
    - Import `setLookbackDays`, `getLookbackDays` from `@src/services/onboarding`

    Place `<MetricsCard onSettingsPress={openLookbackSheet} />` directly after the `<BalanceCard>` component and before the Quick Actions section.

    Add a BottomSheet for lookback period selection:
    - Ref: `lookbackRef = useRef<BottomSheet>(null)`
    - `openLookbackSheet = () => lookbackRef.current?.expand()`
    - Sheet content: 4 Pressable rows for 30 / 60 / 90 / 180 days
    - Each row: `text-sm font-medium text-foreground py-3 px-4`, active row highlighted with `text-primary-700`
    - On select: call `setLookbackDays(days)`, close sheet
    - Sheet snapPoints: `["30%"]`
    - Title: `t("metrics.lookbackTitle")` in `text-sm font-bold text-foreground px-4 py-3`
    - Sheet background: `bg-surface-200`

    Dashboard visual hierarchy (top to bottom per UI-SPEC):
    1. Error Banner (unchanged)
    2. BalanceCard (unchanged)
    3. **MetricsCard (NEW)**
    4. Quick Actions (unchanged)
    5. Accounts Summary (unchanged)
    6. Recent Transactions (unchanged)

    **2. Modify app/(tabs)/settings.tsx** — Add Redo Setup and Buffer Lookback (per D-12, D-06):

    - Import `resetOnboarding` from `@src/services/onboarding`
    - Import `useRouter` from `expo-router`
    - Import `getLookbackDays` from `@src/services/onboarding`

    Add to the "General" Card section (after existing SettingRow items), with Dividers:

    ```tsx
    <Divider />
    <SettingRow
      icon="🔄"
      label={t("settings.redoSetup")}
      onPress={() => {
        resetOnboarding();
        router.replace("/onboarding/rules" as any);
      }}
    />
    <Divider />
    <SettingRow
      icon="📊"
      label={t("settings.bufferLookback")}
      value={`${getLookbackDays()} ${t("metrics.days")}`}
    />
    ```

    The "Redo Setup" onPress resets onboarding MMKV state and navigates to the onboarding flow entry point (per D-12). The route won't exist until Plan 04, but the button and navigation call are wired now. The app will just show a "route not found" until Plan 04 adds the route — this is acceptable since we're building incrementally.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/DoinikHishab && grep -c "MetricsCard" app/\(tabs\)/index.tsx && grep -c "redoSetup\|resetOnboarding" app/\(tabs\)/settings.tsx && grep -c "bufferLookback\|getLookbackDays" app/\(tabs\)/settings.tsx && bun run test --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import.*MetricsCard" app/\(tabs\)/index.tsx` finds the import
    - `grep "<MetricsCard" app/\(tabs\)/index.tsx` finds the component usage
    - `grep "BottomSheet\|lookback" app/\(tabs\)/index.tsx` finds lookback period sheet
    - `grep "resetOnboarding" app/\(tabs\)/settings.tsx` finds the import
    - `grep "redoSetup" app/\(tabs\)/settings.tsx` finds the setting row
    - `grep "bufferLookback\|getLookbackDays" app/\(tabs\)/settings.tsx` finds lookback display
    - `bun run test` passes (no regressions)
  </acceptance_criteria>
  <done>
    Dashboard shows MetricsCard with Age of Money (trend arrow) and Days of Buffering below the BalanceCard. Settings gear opens bottom sheet to select lookback period (30/60/90/180 days). Settings screen has "Redo Setup" row that resets onboarding and navigates to onboarding flow, plus "Buffer Lookback" row showing current period.
  </done>
</task>

</tasks>

<verification>
1. Dashboard renders MetricsCard with mock Age of Money and Days of Buffering values
2. MetricsCard shows "-- days" placeholder when data is null
3. Settings gear on MetricsCard opens lookback period bottom sheet
4. Settings screen shows "Redo Setup" and "Buffer Lookback" rows
5. `bun run test -- MetricsCard RuleCarousel` — component tests pass
6. `bun run test` — full suite passes
7. `bun run lint` — no new Biome errors
</verification>

<success_criteria>
- MetricsCard shows Age of Money with green/red trend arrow and Days of Buffering on dashboard
- MetricsCard and RuleCarousel have passing render tests
- Lookback period is configurable via bottom sheet (30/60/90/180 days) persisted to MMKV
- RuleCarousel swipes through 4 YNAB rule cards with animated pagination dots
- StepIndicator shows active/completed/inactive dots with spring animation
- CategoryTemplateSelector shows 2x2 grid with selection state
- Settings has "Redo Setup" and "Buffer Lookback" rows
- All text uses i18n t() for Bengali/English support
</success_criteria>

<output>
After completion, create `.planning/phases/03-budget-ideology-onboarding/03-03-SUMMARY.md`
</output>
